import { ThObjLoaderPipe } from './th-obj-loader.pipe';

describe('ThObjLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new ThObjLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
