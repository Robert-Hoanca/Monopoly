import { ThObjLoaderDirective } from './th-obj-loader.directive';

describe('ThObjLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ThObjLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
